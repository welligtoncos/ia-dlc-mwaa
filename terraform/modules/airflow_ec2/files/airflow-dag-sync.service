[Unit]
Description=Sync Airflow DAGs from S3 artifact bucket
After=network-online.target docker.service

[Service]
Type=oneshot
ExecStart=/bin/bash -c 'set -a && source /etc/airflow-ec2.env && set +a && aws s3 sync "s3://${ARTIFACT_BUCKET}/dags/" /opt/airflow-ec2/dags/ --region "${AWS_REGION}"'
